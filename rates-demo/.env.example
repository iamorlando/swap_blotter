# Database connection string for Prisma
# For Vercel Postgres, use the pooled connection string. Example:
# postgresql://default:password@HOST:PORT/verceldb?sslmode=require
DATABASE_URL=""

# Optional: override Pyodide CDN base and module URL for server API route
# PYODIDE_BASE="https://cdn.jsdelivr.net/pyodide/v0.26.1/full/"
# PYODIDE_URL="https://cdn.jsdelivr.net/pyodide/v0.26.1/full/pyodide.mjs"

# Path to the DuckDB database file relative to the app CWD (rates-demo)
# Defaults to "../swaps.duckdb" if not set
DUCKDB_FILE="../swaps.duckdb"
