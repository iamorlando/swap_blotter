datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Swap {
  id       Int     @id @default(autoincrement())
  name     String
  maturity String
  pv       Float
}

